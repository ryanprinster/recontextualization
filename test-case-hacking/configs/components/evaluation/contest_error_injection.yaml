# @package evaluation
# LiveCode evaluation configuration
_target_: src.configs.evaluation.EvaluationConfig

defaults:
  - /components/generation: evaluation

# LiveCode contexts
contexts: ["standard", "hack", "do_not_hack"]

# LiveCode metrics
metrics: ["accuracy", "score"]

# Evaluation settings
n_samples: null
save_results: true
save_rollout_messages: false