# @package evaluation
# Multiple choice evaluation configuration
_target_: src.configs.evaluation.EvaluationConfig

defaults:
  - /components/generation: evaluation

# Standard evaluation contexts
contexts: ["standard", "hack", "do_not_hack"]

# Standard evaluation metrics
metrics: ["hacking_rate", "score"]

# Evaluation settings
n_samples: null
save_results: true
save_rollout_messages: true
