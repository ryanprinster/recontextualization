# @package _global_
defaults:
  - /components/model: qwen3_4b
  - /components/dataset: code_generation
  - /components/evaluation: code_generation
  - _self_
seed: 42
dataset_seed: ${seed}
training_seed: ${seed}
dataset:
  use_incorrect_tests: true
sample_source: "all"
hydra:
  run:
    dir: experiments/evaluation/code_generation_qwen3_4b_think/${now:%Y-%m-%d_%H-%M-%S}
